import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GoogleGram1905762',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
